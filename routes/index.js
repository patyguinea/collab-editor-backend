const conversations = require('./conversations');
const home = require('./home');
const info = require('./info');
const ping = require('./ping');
const mutations = require('./mutations');

module.exports = { conversations, home, info, ping, mutations };
